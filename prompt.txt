Features needed to implement/redesign/fix.
- While creating any project, there will be a project description box under project name box. (Optional)
- While creating any project, there will be a project logo selector, from it any user can select their project logo (image, support multiple types of image extensions).
- Project logo will be shown in Project Settings, overview, user can change it or remove it.
- Project logo will be shown while opening Community Creation or Dashboard.
- While creating any project, after selecting server software, version selector will come where the user can put version, user can put multiple versions, version range is 1.8-1.21.x
- Add the version in project menu's overview.
- From now while creating project, users can be able to select both, Gradle & Maven.
- in project settings stats, instead of showing "file actions", show "files" (number).
- In project settings, create a new tab under stats & settings, called compiler, from overview transfer "Language" "Java Version" "Build Tool(s)" there.
- In "images/" folder you can see the image that the project settings in mobile is looking dull, glumpy. Fix it by creating 2 modes, mobile & desktop.
- In "images/" folder you can see that in project setting, both top left (Back to Workspace) and top Right (Open Workspace), the buttons are doing same job, change the top left (Back to workspace) button to (back to dashboard)
- in project workspace, top add a button, by clicking it the below "Chat" & "Code" will swap, means as default "Chat > Files > Codes" comes below, then "Codes > Files > Chat" will come. Make sure to Save it properly, page refresh, page close or browser change will not affect that setting.
- in "images/" folder you can see another image where I opened a project from community, but it opens up the file tree 1st. Make sure to arrange it by the setting of that project, (Chat > Files > Codes/Codes > Files > Chat). From Community Creation, users can not edit that setting.
- in "images/" folder you can see that when any user opens files, the file tree is opened by default (community + workspace), Change it, by default the file trees will be closed.
- in "images/" folder, you can see that any user is unable to open project settings from workspace, add a button in opened workspace top, clicking it will transfer the user to project settings.
- in "images/" folder you can see that when any type of download button is unavailable, properly add a download button, from that users can download the project file. (As soon as the user presses download, it compresses the project into .zip, the .zip will be deleted in 10 minutes after)
