Now you are redy to improve the AuroraCraft. AuroraCraft is a Agentic AI Web APP. Users can create minecraft plugins from here. Whenever any user send any request to AuroraCraft, it sends the msg to OpenCode and then bridge the response back to chat. Every AuroraCraft user will have their own home in System, "/home/auroracraft-user_name/". Whenever a any user creates a project, it will be created at "/home/auroracraft-user_name/project_link_id/". Whenever any user msg the AI first time in chat, AuroraCraft will create a new session in OpenCode and send msg to OpenCode for task execution. (It will run OpenCode at "/home/auroracraft-user_name/project_link_id/") The new session name will be the project_link_id. Whenever the user send another msg to the chat, AuroraCraft find that session and send the new msg to that session. AuroraCraft will currently use free models provided by OpenCode, "MiniMax M2.5" & "big Pickle". OpenCode and the AI Models will show in Admin Panel, "ClaudeCode", "Codex" will be added in future. Now let me say about about exact bridge. All the thinking will be shown properly in a file badge with live update in chat. When OpenCode starts thinking, AuroraCraft will create thinking file badge in chat, when it think anything, the text will appear with live update. (It will never work like that, OpenCode ends the entire thinking then the thinking badge appear) As default when OpenCode is doing thinking, the thinking badge will be opened, as soon as it ends thinking, the badge will be closed. When OpenCode is creating/updating/renaming/deleting any file, the file badge will appear with "creating/updating/renaming/deleting/reading file_path/file_name ...."  with gray colour. When it ends creating/updating/renaming/deleting any file it will show "created/updated/renamed/deleted/read file_path/file_name" with green, orange, yellow, red, blue colour as needed. As soon as it did any file change it will be shown in project workspace fire tree. When OpenCode is responding, AuroraCraft will properly bridge it to the chat by live text update. (It will not wait for OpenCode to send the entire response). Whenever OpenCode create a to-do-list, it will be created in the chat, with completed tasks, ongoing task and remaining tasks with live update. Last msg: AuroraCraft will not wait for OpenCode to complete the entire task and Dump it to chat, it will show and dump everything in chat with live text by text update with file, thinking badges, to-do-list as needed. Make sure that the chat support H1, H2, H3 .. Bold, Italic, Strikethrough etc etc all .md file formats. At last do testing by creating a new project, reviewing it in home, sending simple "hi" msg, and full RTP Plugin generation text with live update from OpenCode sending msg "Create a RTP Minecraft Plugin". Think step by step, do huge deep proper internet research, do end to end testing and make it properly.